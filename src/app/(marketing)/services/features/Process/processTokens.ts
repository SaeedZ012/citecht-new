/**
 * Shared light-surface tokens for the Workflow showcase — standard semantic
 * tokens that sit on a white/background section (same approach as Tech Stack).
 */

/** Secondary text (descriptions, captions, muted labels). */
export const PROCESS_TEXT_MUTED = "text-muted-foreground";

/** Hairline borders on the light surface. */
export const PROCESS_HAIRLINE = "border-border";

/** Panel surface for metric cards and the highlights strip. */
export const PROCESS_PANEL = "border-border bg-surface";
